interface:
  display_name: "Openapi Builder"
  short_description: "Repo-specific patterns and workflows for openapi-builder"
  default_prompt: "Use the openapi-builder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true